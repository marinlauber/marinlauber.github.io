header:

  - titles:
      # @start locale config
      en      : &EN       Post
      fr      : &FR       Blog
      # @end locale config
    url: /blog.html

  - titles:
      # @start locale config
      en      : &EN       Research
      fr      : &FR       Recherche
      # @end locale config
    url: /_pages/research.html

  - titles:
      # @start locale config
      en      : &EN       About
      fr      : &FR       À propos
      # @end locale config
    url: /about.html

  - titles:
      # @start locale config
      en      : &EN       Publications
      fr      : &FR       Publications
      # @end locale config
    url: /_pages/publications.html

  - titles:
      # @start locale config
      en      : &EN       Random Projects
      fr      : &FR       Projets Aléatoires
      # @end locale config
    url: /_pages/random.html
