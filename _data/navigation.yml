header:

  - titles:
      # @start locale config
      en      : &EN       Projects
      fr      : &FR       Projets
      # @end locale config
    url: /blog.html

  - titles:
      # @start locale config
      en      : &EN       Research
      fr      : &FR       Recherche
      # @end locale config
    url: /_pages/research.html

  - titles:
      # @start locale config
      en      : &EN       About
      fr      : &FR       À propos
      # @end locale config
    url: /about.html

  - titles:
      # @start locale config
      en      : &EN       Publications
      fr      : &FR       Publications
      # @end locale config
    url: /_pages/publications.html

  - titles:
      # @start locale config
      en      : &EN       Gallery
      fr      : &FR       Galerie
      # @end locale config
    url: /_pages/gallery.html

  - titles:
      # @start locale config
      en      : &EN       Repositories
      fr      : &FR       Dépôts
      # @end locale config
    url: /_pages/repositories.html
